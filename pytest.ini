[pytest]
# Root-level pytest config — only applies when pytest is run from the project root.
# For backend tests, run pytest from inside backend/ (it has its own pytest.ini).
markers =
    integration: mark test as live network integration test
    live: mark test as requiring live web connection
asyncio_mode = auto
